/*

<-- Cosnt vs Var vs let -->

diferencia entre let y var es el ambito, siendo que "let" se realiza un ambito local
mientras que "var" se crea en un ambito global y "const" crea una constate que no puede
ser redifenida en un ambito local, y tiene un consumo de memoria menor que las var y let

<-- Templete string -->
Esta forma de declarar strings en las variables, para esto se tienen que poner la comilla
invertida:

  ``
Permite agregar directamente variables o bien ejecutar codigo de javascript al momento
de la declaración:

por ejemplo:

`${1 + 1} Resultado de 1+1`

Sin necesidad de concatenar

Otra de sus utilidades es que se puede utilizar en multilinea para poder agregar más codigo
o texto a la variable

<-- Desestructuracion de Objetos -->

Ver ejemplo desestructuracion.js en Javascript

<-- Funciones flecha -->

Una caracteristica importante de las funciones flecha, es que, en caso de que solo exista una
linea, que es la del return, se puede simplificar el código de:

const sumar = (a, b) = {
  return a+b;
}

a

const sumar = (a, b) => a+b;

<-- Callbacks -->



*/
