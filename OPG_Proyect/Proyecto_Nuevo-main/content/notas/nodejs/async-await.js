/*

El await hace que javascript espere el regreso de las promesas para poder continuar con la ejecución normal, el
unico inconveniente de utilizar el await es que este tiene que estar dentro de una función o metodo asincrono.

El async permite convertir una función a una promesa y permite tener así el manejo de diferentes promesas dentro
de su cuerpo.
*/
